<Project>

  <PropertyGroup>
    <RepositoryUrl>https://github.com/totallymoney/FsCDK</RepositoryUrl>
    <PackageProjectUrl>https://totallymoney.github.io/FsCDK/</PackageProjectUrl>
    <DisableImplicitFSharpCoreReference>true</DisableImplicitFSharpCoreReference>
    <DisableImplicitSystemValueTupleReference>true</DisableImplicitSystemValueTupleReference>
    <!-- Example ultra-simple styling and generation settings for FsDocs default template-->
    <FsDocsLogoLink>https://github.com/totallymoney/FsCDK</FsDocsLogoLink>
    <FsDocsLicenseLink>https://github.com/totallymoney/FsCDK/blob/main/LICENSE</FsDocsLicenseLink>
    <FsDocsReleaseNotesLink>https://github.com/totallymoney/FsCDK/blob/main/CHANGELOG.md</FsDocsReleaseNotesLink>
  </PropertyGroup>

  <!-- Shared NuGet Package info -->
  <PropertyGroup>
    <Authors>Edgar Gonzalez</Authors>
    <PackageTags>F#;Declarative;AWS;CDK</PackageTags>
    <!-- README.md is packed from the repository root (see the fsproj None Include) -->
    <PackageReadmeFile Condition="'$(IsPackable)' == 'true'">README.md</PackageReadmeFile>
    <!-- Must match the repository LICENSE file (MIT) -->
    <PackageLicenseExpression>MIT</PackageLicenseExpression>
    <SymbolPackageFormat>snupkg</SymbolPackageFormat>
  </PropertyGroup>

  <PropertyGroup>
    <OtherFlags>$(OtherFlags) --test:GraphBasedChecking --test:ParallelOptimization --test:ParallelIlxGen --strict-indentation+ --realsig+</OtherFlags>
    <ProduceReferenceAssembly>true</ProduceReferenceAssembly>
  </PropertyGroup>

</Project>
